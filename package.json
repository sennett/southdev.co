{
  "name": "southdev.co",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist/* && node_modules/.bin/parcel build src/index.html --dist-dir dist && cp static/* dist",
    "deploy": "rm -rf ../sennett.github.io/* && cp dist/* ../sennett.github.io && cd ../sennett.github.io && git add --all && git commit -m 'new deploy' && git push -u origin master "
  },
  "devDependencies": {
    "parcel": "^2.0.0-beta.2"
  }
}